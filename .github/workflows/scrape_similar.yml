name: Run Similar Apps Scraper

on:
  schedule:
    - cron: '0 2 * * *' # Runs at 2 AM UTC
  workflow_dispatch: 

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run Similar Apps Scraping Script
        run: python scrape_apps_by_similar.py

      - name: Commit and push changes
        run: |
          if [ -f similar_apps_data.csv ]; then
            git config --local user.email "github-actions[bot]@users.noreply.github.com"
            git config --local user.name "github-actions[bot]"
            git add similar_apps_data.csv
            
            if ! git diff --staged --quiet; then
              git commit -m "Automated update of similar_apps_data.csv"
              git pull origin master --rebase
              git push origin master
            else
              echo "No changes detected. Skipping commit."
            fi
          else
            echo "CSV file not found - scraping may have failed"
            exit 1
          fi

      - name: Upload to cPanel via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: rankmyapps.com
          username: rankyggl
          password: gEK3g86pOdnH
          port: 21
          local-dir: ./
          server-dir: /public_html/wp-content/themes/astra-child/
          exclude: |
            **/*
            !similar_apps_data.csv
